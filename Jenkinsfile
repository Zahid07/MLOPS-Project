pipeline {
    agent any

    environment {
        PATH = "${PATH}:${HOME}/.local/bin"
    }

    stages {
        stage('Cloning the repo') {
            steps {
                // just print a message
                echo 'Cloning the repo'

            }
        }

        

    }
}
